fragment MeMdFields on Me {
    ...MeSmFields
    birthDate
    uni
    matchingEnabled
    matchQuality
    manualEnabled
    lastAutoMatched
    autoFreq
    nextManualMatchId
    matchStudents
    snoozedUntil
}
