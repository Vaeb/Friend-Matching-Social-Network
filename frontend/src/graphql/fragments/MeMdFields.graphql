fragment MeMdFields on Me {
    ...MeSmFields
    birthDate
    uni
    uniConfirmed
    matchingEnabled
    matchQuality
    manualEnabled
    lastAutoMatched
    autoFreq
    nextManualMatchId
    studentsOnly
    snoozedUntil
}
