query GetPostsFromFriends($limit: Int) {
    getPostsFromFriends(limit: $limit) {
        id
        text
        creator {
            id
            username
            name
        }
        createdAt
    }
}
