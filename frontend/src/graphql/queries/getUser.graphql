query GetUser($userId: Int!) {
    getUser(userId: $userId) {
        id
        username
        name
        visInterests
        createdAt
        areFriends
        friendDate
        haveMatched
        matchDate
    }
}
