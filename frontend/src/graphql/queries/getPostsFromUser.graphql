query GetPostsFromUser($userId: Int!, $limit: Int) {
    getPostsFromUser(userId: $userId, limit: $limit) {
        id
        text
        creator {
            ...UserSmFields
        }
        createdAt
    }
}
