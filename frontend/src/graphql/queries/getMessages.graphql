query GetMessages($target: Int!) {
    getMessages(target: $target) {
        id
        messages {
            id
            text
            from {
                id
            }
            to {
                id
            }
            createdAt
        }
    }
}
