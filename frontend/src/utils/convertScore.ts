// export const sliderToProper = (sliderValue: number) => (sliderValue + 100) / 2;
// export const properToSlider = (properValue: number) => properValue * 2 - 100;

export const sliderToProper = (sliderValue: number) => sliderValue;
export const properToSlider = (properValue: number) => properValue;
